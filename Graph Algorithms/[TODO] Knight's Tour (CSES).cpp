// Knight's Tour (CSES)
 
#include <iostream>
#include <vector>
 
using namespace std;
 
void solve(int x, int y) {
    
}
 
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int x, y;
    cin >> x >> y;

    solve(x, y);
    
    return 0;
}