// Meet in the Middle (CSES)

#include <iostream>
#include <vector>

using namespace std;

// Time: O(2^(N/2))
// Space: O(2^(N/2))

int solve(int N) {
    return -1;
}

int main() {
    int N;
    cin >> N;

    solve(N);

    return 0;
}