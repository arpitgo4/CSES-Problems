// Rectangle Cutting (CSES)

#include <iostream>
#include <vector>

using namespace std;

void solve(int l, int w) {

}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int l, w;
    cin >> l >> w;

    solve(l, w);

    return 0;
}