// Range Xor Queries (CSES)

#include <iostream>
#include <vector>

using namespace std;

// NOTE: No idea what to use here... Segment Tree or Square Root Decomposition

void solve() {

}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int N, Q;
    cin >> N >> Q;

    vector<int> A(N);
    for (int i = 0; i < N; i++)
        cin >> A[i];



    int a, b;
    for (int i = 0; i < Q; i++) {
        cin >> a >> b;

    }


    return 0;
}